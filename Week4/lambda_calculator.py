add = lambda x, y: x + y
substract = lambda x, y: x - y
multiply = lambda x, y : x * y
divine = lambda x, y: x / y
if __name__ == "__main__":
    x = 5
    y = 4
    print(add(x, y))
    print(substract(x,y))
    print(multiply(x, y))
    print(divine(x, y))
    